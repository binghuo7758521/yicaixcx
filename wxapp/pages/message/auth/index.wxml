<view class="page">
    <view class="inner">
        <view class="image">
            <image src="/static/images/accredit.png"></image>
        </view>
        <view class="text" wx:if="{{!close}}">查看系统内容需确认登录信息</view>
        <button bindtap="bind" class="btn" openType="getUserInfo" wx:if="{{!close}}">点击登录</button>
        <view class="text" wx:if="{{close}}">{{text}}</view>
    </view>
</view>
